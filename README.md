# Homebrew Tap

Homebrew formulas for Emanuel Casco projects.

## ocgo

After a release is published from `emanuelcasco/ocgo`, install with:

```bash
brew install emanuelcasco/tap/ocgo
```

Or:

```bash
brew tap emanuelcasco/tap
brew install ocgo
```
